# ──────────────────────────────────────────────
# Modern terminal & colors
# ──────────────────────────────────────────────
# Use tmux's own terminfo to support italics + 24-bit colors
set -g default-terminal "tmux-256color"

# Enable true color and italics
set -as terminal-overrides ',xterm-256color:Tc'
set -as terminal-overrides ',xterm-256color:RGB'
set -as terminal-overrides ',*:sitm=\E[3m'

# History limit
set -g history-limit 20000

# ──────────────────────────────────────────────
# Keyboard / Mouse
# ──────────────────────────────────────────────
setw -g mode-keys vi
set -g mouse on

# Do not allow automatic rename of windows
setw -g allow-rename off

# Automatically renumber tmux windows
set -g renumber-windows on

# ──────────────────────────────────────────────
# Prefix & bindings
# ──────────────────────────────────────────────
unbind C-b
set -g prefix C-a
bind C-a last-window
bind-key a send-prefix  # For nested tmux

# Faster pane switching
set -sg escape-time 0

# Window/pane index starts at 1
set -g base-index 1
setw -g pane-base-index 1

# Split windows in current path
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind _ split-window -v -l 11

# Quickly open a new window                                                                                                      │    │
bind N new-window

# Pane movement shortcuts (vim-like)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes quickly
bind -r H resize-pane -L 10
bind -r J resize-pane -D 10
bind -r K resize-pane -U 10
bind -r L resize-pane -R 10

# Synchronize panes
bind-key * setw synchronize-panes

# Reload config
bind R source-file ~/.tmux.conf \; display-message "Reloaded!"

# ──────────────────────────────────────────────
# Copy & Paste (macOS compatible)
# ──────────────────────────────────────────────
# vim-like copy mode
bind-key Escape copy-mode

# If you are on macOS, pbcopy works directly in tmux 3.x without reattach-to-user-namespace
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'V' send -X select-line
bind-key -T copy-mode-vi 'r' send -X rectangle-toggle
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel "pbcopy"

# ──────────────────────────────────────────────
# Visual tweaks
# ──────────────────────────────────────────────
set -g status-justify left
set -g status-left '#[bg=colour72] #[bg=colour237] #[bg=colour236] #[bg=colour235]#[fg=colour185] #S #[bg=colour236] '
set -g status-left-length 16
set -g status-bg colour237
set -g status-right '#[bg=colour236] #[bg=colour235]#[fg=colour185] #(date "+%a %b %d %H:%M") #[bg=colour236] #[bg=colour237] #[bg=colour72] '
set -g status-interval 60

set -g pane-active-border-style fg=colour240
set -g pane-border-style fg=colour238

setw -g window-status-format '#[bg=colour238]#[fg=colour107] 🙈 #I #[bg=colour239]#[fg=colour110] #[bg=colour240]#W#[bg=colour239]#[fg=colour195]#F#[bg=colour238] '
setw -g window-status-current-format '#[bg=colour236]#[fg=colour215] 🙉 #I #[bg=colour235]#[fg=colour167] #[bg=colour234]#W#[bg=colour235]#[fg=colour195]#[bg=colour236] '
